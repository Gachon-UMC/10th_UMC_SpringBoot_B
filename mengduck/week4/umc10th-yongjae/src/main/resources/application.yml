spring:
  application:
    name: umc10th-yongjae

  datasource:
    url: jdbc:mysql://localhost:3306/UMC_PROJ1?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: spring
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    show-sql: true